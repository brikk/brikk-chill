plugins {
    id("chill-kotlin-library")
}

// no dependencies: this artifact is safe to add to any project that only wants the markers

description = "Marker annotations for chill build-time lambda verification scopes"
